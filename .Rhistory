comb1 = paste(curr.album, curr.song)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
curr.song
comb1 = paste(curr.album, curr.song)
comb1
?str_sub
str_sub("hello", start=1, end=-1)
str_sub("hello", start=1, end=-2)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -4)
track.name
rack.name
track.name
track.name
track.name
track.name
track.name
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -4)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.name
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
print(track.split)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
print(track.split)
track.name = track.split[3]
print(track.name)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
print(track.split)
track.name = track.split[1]
print(track.name)
source("~/Documents/GitHub/lab-2-using-libraries-and-objects-in-r-brendanmariano/Untitled.R", echo=TRUE)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
print(track.split)
print(track.split[1])
source("~/Documents/GitHub/lab-2-using-libraries-and-objects-in-r-brendanmariano/Untitled.R", echo=TRUE)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
# print(track.split)
print(track.split[1])
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
# print(track.split)
print(track.split[3])
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
# print(track.split)
print(track.split[3])
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
# print(track.split)
print(track.split[2])
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[1]
track.name = track.name[3]
print(track.split)
print(track.split[1])
track.name = track.split[1]
print(track.name)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[1]
print(track.name)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[1]
print(track.name)
print(track.name[1])
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[3]]
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[1]]
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[1]]
print(track.name)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
print(track.name)
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[1]]
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[3:3]]
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[3]]
library("stringr")
storedM = list.dirs(path = "MUSIC", full.names = TRUE, recursive = TRUE)
print(storedM)
num = str_count(storedM,"/")
num
storedM[which(num == 2)]
album.dirs <- storedM[which(num == 2)]
#num2 = str_count(stored_files)
curr.album <- album.dirs[1]
#1
stored_files = list.files(path = curr.album)
num1 = str_count(stored_files, ".wav")
num1
song.files <- stored_files[which(num1 == 1)]
#2
code.to.process = vector(mode = "character", length = 0)
curr.song = song.files[1]
comb1 = paste(curr.album, curr.song)
track.name = str_sub(curr.song, start = 1, end = -5)
track.split = str_split(track.name, "-")
track.name = track.split[[1]]
track.name = track.name[3]
print(track.name)
comb1
comb1
?paste
